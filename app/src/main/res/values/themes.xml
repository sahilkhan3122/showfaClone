<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ShowfaDriverLetest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/theme_color</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.ShowfaDriverLetest" parent="Base.Theme.ShowfaDriverLetest" />
    <style name="TextLabel" parent="Theme.MaterialComponents">
        <item name="android:textSize">@dimen/_12dp</item>
        <item name="colorControlNormal">@color/theme_color</item>
        <item name="colorControlActivated">@color/theme_color</item>
    </style>
    <style name="text_semibold">
        <item name="android:textSize">@dimen/_14sp</item>
        <item name="fontFamily">@font/gibson_semibold2</item>
        <item name="android:textColor">#bb000000</item>
    </style>

    <style name="AlertDialogCustoms" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:textColor">@color/black</item> <!--Title text color-->
        <item name="android:textColorPrimary">@color/black_33</item> <!--Item text color-->
        <item name="android:textColorSecondary">@color/color_yellow</item> <!--If you need change separator color-->
        <item name="android:buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="android:background">@color/white</item>
        <item name="android:textAllCaps">false</item>
        <item name="fontFamily">@font/gibson_regular</item>

    </style>
</resources>